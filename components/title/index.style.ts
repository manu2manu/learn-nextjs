import styled from "styled-components";

export const TitleWrapper = styled.span`
  align-self: center;
  font-size: 1.7em;
  font-weight: bold;
  margin-bottom: 25px;

  @media(min-width: 768px) {
    font-size: 2.7em;
  }
`;
